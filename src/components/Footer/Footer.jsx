import React from 'react'
import css from './Footer.module.css'

const Footer = () => {
  return (
    <div className={css.Footer}>
        <label className={css.text}>The sky is not the limit</label>
    </div>
  )
}

export default Footer